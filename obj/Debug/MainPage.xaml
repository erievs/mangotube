<Page
    x:Class="ValleyTube.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:ValleyTube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>
        <Pivot x:ConnectionId='1' Title="ValleyTube"                                          >
            <PivotItem Header="Trending">
                <ListView x:ConnectionId='12' x:Name="TrendsListView"                                     >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="10">
                                <Button x:ConnectionId='13' Background="Transparent" BorderBrush="Transparent" Padding="0"                          >
                                    <Image Source="{Binding ThumbnailUrl}" Width="120" Height="90" Margin="0,0,10,0" Stretch="UniformToFill"/>
                                </Button>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                    <TextBlock Text="{Binding Author}" FontSize="14" Foreground="Gray"/>
                                    <TextBlock Text="{Binding ViewCountText}" FontSize="14" Foreground="Gray"/>
                                    <TextBlock Text="{Binding PublishedText}" FontSize="14" Foreground="Gray"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>

            <PivotItem Header="Gaming">
                <ListView x:ConnectionId='10' x:Name="GamingListView"                                     >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="10">
                                <Button x:ConnectionId='11' Background="Transparent" BorderBrush="Transparent" Padding="0"                          >
                                    <Image Source="{Binding ThumbnailUrl}" Width="120" Height="90" Margin="0,0,10,0" Stretch="UniformToFill"/>
                                </Button>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                    <TextBlock Text="{Binding Author}" FontSize="14" Foreground="Gray"/>
                                    <TextBlock Text="{Binding ViewCountText}" FontSize="14" Foreground="Gray"/>
                                    <TextBlock Text="{Binding PublishedText}" FontSize="14" Foreground="Gray"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>
            <PivotItem Header="Music">
                <ListView x:ConnectionId='8' x:Name="MusicListView"                                     >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="10">
                                <Button x:ConnectionId='9' Background="Transparent" BorderBrush="Transparent" Padding="0"                          >
                                    <Image Source="{Binding ThumbnailUrl}" Width="120" Height="90" Margin="0,0,10,0" Stretch="UniformToFill"/>
                                </Button>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                    <TextBlock Text="{Binding Author}" FontSize="14" Foreground="Gray"/>
                                    <TextBlock Text="{Binding ViewCountText}" FontSize="14" Foreground="Gray"/>
                                    <TextBlock Text="{Binding PublishedText}" FontSize="14" Foreground="Gray"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>
            <PivotItem Header="Movies">
                <ListView x:ConnectionId='6' x:Name="MoviesListView"                                     >
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Orientation="Horizontal" Margin="10">
                                <Button x:ConnectionId='7' Background="Transparent" BorderBrush="Transparent" Padding="0"                          >
                                    <Image Source="{Binding ThumbnailUrl}" Width="120" Height="90" Margin="0,0,10,0" Stretch="UniformToFill"/>
                                </Button>
                                <StackPanel>
                                    <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                    <TextBlock Text="{Binding Author}" FontSize="14" Foreground="Gray"/>
                                    <TextBlock Text="{Binding ViewCountText}" FontSize="14" Foreground="Gray"/>
                                    <TextBlock Text="{Binding PublishedText}" FontSize="14" Foreground="Gray"/>
                                </StackPanel>
                            </StackPanel>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </PivotItem>
            <PivotItem Header="Search">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="*" />
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>

                    <TextBox x:ConnectionId='2' x:Name="SearchBox" PlaceholderText="Search..." Margin="10"                                     Grid.Row="0"/>

                    <ListView x:ConnectionId='3' x:Name="SearchListView" Margin="10" Grid.Row="1"                                     >
                        <ListView.ItemTemplate>
                            <DataTemplate>
                                <StackPanel Orientation="Horizontal" Margin="10">
                                    <Button x:ConnectionId='5' Background="Transparent" BorderBrush="Transparent" Padding="0"                           >
                                        <Image Source="{Binding VideoThumbnails[0].Url}" Width="120" Height="90" Margin="0,0,10,0" Stretch="UniformToFill"/>
                                    </Button>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Title}" FontSize="20" FontWeight="Bold" Foreground="White"/>
                                        <TextBlock Text="{Binding Author}" FontSize="14" Foreground="Gray"/>
                                        <TextBlock Text="{Binding ViewCountText}" FontSize="14" Foreground="Gray"/>
                                        <TextBlock Text="{Binding PublishedText}" FontSize="14" Foreground="Gray"/>
                                    </StackPanel>
                                </StackPanel>
                            </DataTemplate>
                        </ListView.ItemTemplate>
                    </ListView>

                    <Button x:ConnectionId='4' x:Name="LoadMoreButton" Content="Load More" Margin="10" VerticalAlignment="Bottom" Grid.Row="2"                             />
                </Grid>
            </PivotItem>
        </Pivot>
    </Grid>
</Page>

